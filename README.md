# Julian Mendonca — Resume site

Single static page (`index.html` + `portrait.jpg`), no build step. Deployed to GitHub Pages from `main` by `.github/workflows/deploy.yml`.

The previous Next.js version of this site is preserved in git history (tag `legacy-next-site`).
